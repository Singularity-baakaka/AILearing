# MLP基础理解笔记
## 1.MLP,或者单层神经网络，可以用最简单的函数拟合模式理解：假设我们拟合Lnx，那么，就是用一堆非线性激活函数的线性组合来逼近这个函数，就好像在坐标轴上放上一堆函数，有高有低，位置不同，然后，通过训练调节它们的位置，最终让他们拼起来，逼近目标函数。这个过程，就是神经网络的最基础。
## 2.多层神经网络的话，在此之前我们需要引入神经网络在脑海里的结构：一个神经元具有多个或一个输入和一个输出。在单层神经网络拟合Lnx时候，每个神经元具有一个输入，一个输出。最后的输出层是一个神经元，多个输入。这时候结合我们的“山包”形象思维，就是把它们叠加起来。而对于多层。第一层假设有十个神经元。第二层10个，每个神经元有十个输入，此时，第二层神经元的意义就在于，把第一层的激活函数线性组合，成为一条新的“工具函数”，第二层十个神经元，就相当于构建了十个“工具函数”，然后输出神经元再对它们进行线性组合，就能够形成更多特征，让拟合更加精确多样。
# SVD基础理解笔记

## 1.SVD是什么，从线性变换的角度来说，SVD可以把一个矩阵分解为三个矩阵的乘积：两个正交矩阵和一个对角矩阵。这三个矩阵可以看作是线性变换的三个步骤：先旋转，再拉伸，再旋转。而假设我们用SVD进行信息处理，那么最为重要的一点是：
### 对于信息构成的矩阵，最重要的不是线性变化的输入和输出，而是线性变换本身。信息就蕴含在线性变换本身中。这句话是很深刻的，所以需要我们在以后学习的时候着重理解！

## 2.为什么SVD能表征信息？假设图片中的一块区域是空白的（都是0），那么在线性变换中，这块区域对应的坐标分量就几乎没什么变化了，所以在SVD中，对于这个方向的变换就小，体现出来就是，奇异值小，信息小。基于此可以作更深的理解，不过这里没有更深入赘述的必要。

## 3.协方差矩阵：这是个对称阵，对角线是变量对于自身的方差，第i行，第j列的元素是变量i和变量j的方差。有点像我们以前的注意力矩阵。协方差可以描述变量之间的相关性。为什么呢，可以由均值不等式得出，协方差是X，Y的误差相乘，显然，两者同步变化时候（相等）它们的乘积最大。因此可描述相关性。然后，协方差矩阵和一个向量U相乘，是沿着数据散开的方向拉这个向量。假设，随机变量向量是4维的，那么总体的样本就是分布在四维空间中的一个点云。U是四维空间中的一个向量。UT乘以协方差矩阵再乘以U，就能得到这些点云在U上投影点的方差。显然，方差越大，信息就越多。然后，对于协方差矩阵Σ的特征向量，在拉伸后不变，意味着这个向量本身就已经是方差投影最大的方向了...具体的数学证明这里略。另外，Σ具有和其维度相同的特征向量。  

## 4.信息的理解：我们目前只来一点粗陋的理解：信息就是可以用来区分不同事物的特征。显然，一个变量的方差越大，这个变量蕴含的信息也就越多。

## 5.关于SVD的理解：实际上，SVD中的Σ和VT，和协方差矩阵的特征值和特征向量是有关联的，而且只差一个比例系数和根号。而U，本质上是AAT（A是样本矩阵），它描述的是每个数据对相互的相似度。也类似注意力矩阵。它的特征向量是，能让样本间差异最大的方向。。这里的数学证明和几何意义有点繁琐。不过，结合SVD的效果，我们可给出一个定性的结论。这个矩阵决定了每个主成分对于整体数据的贡献度。这就是为什么三个矩阵相乘能复原整个矩阵。

# RNN的理解：RNN本质上是在输入上叠加了上一次推理的“记忆”的MLP，它的输入可以是一段时间上的序列，然后根据循环推理，输出下一个时刻的预测值...与MLP的区别是，它在神经元中引入了“记忆权重”，Wh,那么，在前向传播中，我们会输入hprev和当前输入，它们会计算得出一个ht，然后ht会作为下一次的hprev。同时,ht用于计算这一次的预测值。

# 机器学习工具 监督学习：回归问题&分类问题 无监督学习：从没有标签的数据中学习规律 

# 回归问题中，找出使得代价函数最小的参数theta，然后用这个参数去预测新的数据。

# 梯度下降：找的那个极小值是局部的最优解，可能不是全局，此外，更新梯度的时候，是对样本组中所有x计算出来的梯度求和再去更新。这样，保证对全局样本的拟合。（这种全局计算导数的算法叫做批量梯度下降）但是在数据量非常大的情况下，这种算法会很慢。所以引入了随机梯度下降（一次只做一个样本的更新梯度遍历整个样本集），然后还有小批量梯度下降，是从样本集里面分批次，进行梯度下降。相当于是升级版的随机梯度下降，也有遍历数据集的操作。
## 损失函数在考虑全体样本点误差的情况下，是唯一的。是每个样本点的误差的和。这也是为什么随机梯度下降能够达到较好效果的原因。

## 梯度振荡发生在学习率过大的情况下，自己结合直观理解去推导

# 参数学习算法和非参数学习算法，非参数学习意味着参数数量会随着样本数量变化而变化。对于之前的线性回归算法（参数学习），可以引出第一个非参数学习：局部加权线性回归。这相当于在原本的线性回归的代价函数中引入了权重函数。我们想拟合某点的时候，让这个权重函数（通常是钟形曲线）的中心位于这个点。然后，对于每个样本对应的均方误差，就乘以权重。

# 线性回归的概率理解 实际上，残差描述的是“未建模表述的影响”，例如房价，受房子大小，位置，卖家的心情诸多参数影响。但是实际做线性回归建模的时候一般考虑前两个因素。这就带来了残差。残差是模型无法解释的部分。而概率论中对这种多因素影响造成的随机变量的描述是正态分布，因此残差服从正态分布即p(y∣x;θ)=N(y∣θTx,σ2)。若想让这个最大(MLE),则需要最小化平方误差。因此我们使用均方误差。这可以用数学推导

# 分类问题 
## 逻辑回归 sigmoid(x)=逻辑函数(把值转换到0-1间)，分类问题完成的是:输入特征，返还它“是什么”的概率。在这个过程中，我们引入了：P(yi​∣xi​;θ)=(hθ​(xi​))^yi​⋅(1−hθ​(xi​))^(1−yi​)。这个公式描述了在本预测模型下，预测值符合真实值（0or1）的概率。将每个样本的这个P相乘，得到了似然函数，我们让似然函数越大，也就意味着这个模型的预测，会更符合真实结果。这个时候对似然函数求导，就能得到损失函数了。也可以直接对似然函数应用梯度上升。

# 广义线性模型 普通的线性回归的输出是θT*x,是对输入特征的线性组合。而广义线性模型是让y的均值的一个函数等于那个θTX，也就是把线性组合的结果变换到一个我们需要的空间，比如（0-1），用于分类。在以往的线性回归中，我们假定残差是正态分布的，然后对似然函数分析，可得到损失函数用于把似然函数最大化（似然函数最大化的意义就是：所有样本点符合模型的概率最大）。广义线性模型对残差不再要求正态分布，而是让残差的特性完全由响应变量的分布决定——逻辑回归的残差是伯努利误差，泊松回归的残差是计数误差，伽马回归的残差是正数误差。损失函数也不再是均方误差，而是直接从响应变量的分布推导出负对数似然函数。这部分需要一些数学推导，不过对我们来说，没有必要。了解即可。







  